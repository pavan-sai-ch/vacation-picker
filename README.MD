web-app with react and can be used for vacation planner.
automatically sorts places depending on the user location.\

1: install dependencies using package.json with npm install
2. run using npm run dev